import 'package:flutter/material.dart';

class AdminReviewFeedback extends StatelessWidget {
  const AdminReviewFeedback({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}