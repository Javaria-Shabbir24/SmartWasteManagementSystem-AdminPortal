import 'package:flutter/material.dart';

class AdminGridSection extends StatelessWidget {
  const AdminGridSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}